{
  "name": "adem-inside",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "python build_so101.py && tsc --noEmit && vite build && vite build --config vite.hero.config.ts",
    "dev": "vite",
    "build:hero": "tsc --noEmit && vite build --config vite.hero.config.ts",
    "build:go2": "python build_go2.py && tsc --noEmit && vite build --config vite.go2.config.ts"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "gltfpack": "^1.2.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
